package constant;

/**
 * The `OrderAttributes` enumeration represents the valid attributes of the main entity that can be used for searching
 * and parse the JSON files in the JSON statistics parser program.
 */
public enum OrderAttributes {
    ORDER_NUMBER,
    ITEMS,
    TOTAL_AMOUNT,
    CUSTOMER
}
